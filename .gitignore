# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets
.env
.env.*
!.env.example

# Auth state (real session cookies)
.auth/
tests/.auth/

# Playwright
playwright-report/
test-results/
trace.zip

# Manifest — everything generated. registry.yaml names every internal
# URL + login selectors, so we treat it like extractions/plans (local-only).
# The autocoder creates this folder at runtime; no need to track it.
manifest/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
